# Phase 3 — K1/K2/K3 baseline 미달 영역 root cause fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) to implement task-by-task. Checkbox `- [ ]` syntax.

**Goal:** Phase 2 baseline 측정 (`docs/handoff/2026-05-29-baseline-result.md`) 의 미달 KPI (K1.1/K1.2/K1.3/K2.3/K3.1) root cause fix + silent-fail audit P1 정리. Phase 4 재측정 진입 준비. 큰 변경 (ml_place 학습, partial prefetch) 은 deferred (사용자 결정 또는 spec v2 PR).

**Architecture:** baseline 결과의 P0/P1 미달 영역별 분리 task. 회귀 위험 작은 fix (T1/T3) 우선, 중간 위험 (T2/T4/T5/T6) 후순위, 큰 변경 (T8/T9/T10) deferred. 매 task 후 qa-runtime 회귀 검증 (시연 1회 + 영향 영역 runner) 강제.

**Tech Stack:** backend FastAPI + Redis (캐싱), gemini.py (timeout), silent-fail 가시화 패턴, .gstack-k2-runner 확장.

**Spec link:** `docs/superpowers/specs/2026-05-27-exhibition-stability-k1-k2-k3-design.md`
**Phase 2 결과:** `docs/handoff/2026-05-29-baseline-result.md`

---

## 적용 안 한 버전 복귀 안전망 (사용자 안전 가이드)

매 task fix 가 회귀 유발 시 복귀 옵션:

| 시점 | 안전 tag |
|---|---|
| Phase 1 진입 직전 (backend 안정, 시연 GREEN) | `v-pre-phase1-green` (commit `595fa0b`) |
| Phase 1 완료 (산출물만, backend 변경 없음) | `v-phase1-ready` (commit `ca871e5`) |
| Phase 3 진입 직전 (BUG-27-1 fix + baseline 정정 완료) | 본 plan commit 직후 새 tag `v-pre-phase3` |

복귀 명령:
```bash
git revert <fix-SHA>  # 안전 (push 한 commit 도 OK, history 보존)
git reset --hard <safe-tag>  # local 만, push 안 한 commit 폐기
git checkout <SHA> -- <file>  # 특정 파일만 복원
```

---

## File Structure

| 변경 파일 | 책임 |
|---|---|
| `backend/app/services/gemini.py` | timeout 정책 정정 + retry 단축 (T1) |
| `backend/app/services/pipeline/nodes/slot.py:454` | VOTE_OPTIONS_PATCH 외층 try 범위 축소 (T2) |
| `backend/app/services/pipeline/helpers/slots.py:118-119, 161-162, 485-486` | google busy_periods + Redis 분기 narrow except (T3) |
| `backend/app/api/ws/agent.py:213-215, 264-362, 868-872` | snapshot/greeting/trigger try 범위 + narrow except (T4) |
| `backend/app/services/pipeline/nodes/place.py` | place_search 결과 Redis 캐싱 (T5) |
| `backend/app/api/routes/calendar.py` | free-slots 1h Redis 캐싱 (T6, LIMIT-7) |
| `backend/app/api/routes/intents.py` + `.gstack-k2-runner.py` | entity_extraction endpoint 노출 + K2.3 측정 (T7) |

| 신규 handoff | 책임 |
|---|---|
| `docs/handoff/2026-05-30-phase3-progress.md` | Phase 3 fix 진행 + 회귀 검증 + Phase 4 진입 안내 |

---

## Task 1: Gemini timeout 25s → 10s + retry 정책 정정 (P2-6)

baseline: Gemini timeout retry 40% / 최종 실패 10%. timeout 25s 가 너무 길어 사용자 latency 직격.

**Files:**
- Modify: `backend/app/services/gemini.py` (timeout + retry)

- [ ] **Step 1.1**: 현재 timeout/retry 정책 확인
```bash
grep -n "timeout\|retry\|TIMEOUT" backend/app/services/gemini.py | head -10
```

- [ ] **Step 1.2**: timeout 25 → 10s + retry 1회 유지 (총 wait 20s 보장)
- [ ] **Step 1.3**: syntax + docker restart
- [ ] **Step 1.4**: 시연 1회 K1.1 latency 측정 (`.gstack-demo.py --fast`) — 감소 효과 확인
- [ ] **Step 1.5**: Commit + push

`fix(k1.2): Gemini timeout 25s → 10s + retry 1회 (총 wait 20s)`

회귀 위험: 정상 응답 fail 가능성 (Gemini 응답 평균 ~2-5s 라 10s 충분). 시연 1회 검증으로 확인.

---

## Task 2: silent-fail P1 A — slot.py:454 try 범위 축소

audit (`commit f717b06`) Top 4 항목. VOTE_OPTIONS_PATCH 외층 try 30줄+ 의 `warning("...%s", _patch_err)` → exc_info=True 추가 + 책임별 4구역 분리.

**Files:**
- Modify: `backend/app/services/pipeline/nodes/slot.py:320-456` (외층 try → DB / Redis msg_id / message update / WS broadcast 4 구역 분리)

- [ ] **Step 2.1**: 현재 코드 read + 책임 4구역 식별
- [ ] **Step 2.2**: 4구역 분리 + 각 구역 narrow except (`NameError, AttributeError` 명시 re-raise)
- [ ] **Step 2.3**: syntax + docker restart
- [ ] **Step 2.4**: 시연 1회 GREEN (bug-26-g/f 회귀 검증)
- [ ] **Step 2.5**: Commit + push

회귀 위험: bug-26-g/f 회귀 ↑. 시연 검증 필수.

---

## Task 3: silent-fail P1 B — helpers/slots.py google busy_periods (낮은 위험)

audit #14, #15: google_calendar 호출 2개 except 모두 `return []`, Redis 분기 debug swallow.

**Files:**
- Modify: `backend/app/services/pipeline/helpers/slots.py:118-119, 161-162, 485-486`

- [ ] **Step 3.1**: NameError/AttributeError 명시 re-raise + `logger.exception` 승격
- [ ] **Step 3.2**: 시연 1회 + K2 runner 회귀 검증
- [ ] **Step 3.3**: Commit + push

---

## Task 4: silent-fail P1 C — agent.py snapshot/greeting/trigger

audit #6, #8, #10: snapshot push 3개 + greeting emit + trigger_msg_id resolve.

**Files:**
- Modify: `backend/app/api/ws/agent.py:213-215, 264-362, 868-872`

- [ ] **Step 4.1**: 같은 패턴 적용 — debug → exception 승격, narrow except, 범위 축소
- [ ] **Step 4.2**: 시연 1회 + K3 onboarding runner 회귀
- [ ] **Step 4.3**: Commit + push

---

## Task 5: place_search 결과 Redis 캐싱 (P0-1 부분)

K1.3 ACT5 22~30s 의 큰 비중. 동일 (location, meeting_type, headcount) 쿼리 재발화 시 Redis 캐시 hit. TTL 30분.

**Files:**
- Modify: `backend/app/services/pipeline/nodes/place.py`

- [ ] **Step 5.1**: Redis key 설계 — `maedeup:place_cache:{location}:{meeting_type}:{headcount}`, value = JSON serialized place list, TTL 30min
- [ ] **Step 5.2**: place_recommendation 진입 직후 cache get → hit 시 즉시 응답 (Gemini/ML 호출 skip)
- [ ] **Step 5.3**: cache miss → 기존 흐름 + 응답 후 cache set
- [ ] **Step 5.4**: 시연 1회 (cache miss) + 시연 2회 (cache hit) → K1.3 latency 측정 비교
- [ ] **Step 5.5**: Commit + push

회귀 위험: 캐시 stale (Kakao API 결과 변경 빈도). 30min TTL 로 충분 추정.

---

## Task 6: free-slots Redis 캐싱 (P0-1 부분, LIMIT-7)

K1.1 의 free-slots 빌더 latency 감소. (room_id, date_range) 키 캐싱 TTL 5min (가입/거부 발화 시 invalidate 필요).

**Files:**
- Modify: `backend/app/api/routes/calendar.py`

- [ ] **Step 6.1**: Redis key `maedeup:free_slots:{room_id}:{date_range_hash}`, TTL 5min
- [ ] **Step 6.2**: cache get → hit 시 즉시. miss → 기존 + cache set
- [ ] **Step 6.3**: invalidate 트리거 — busy_period 추가/삭제 시 해당 room 의 cache delete
- [ ] **Step 6.4**: K3 시나리오 + 시연 회귀
- [ ] **Step 6.5**: Commit + push

회귀 위험: invalidate 누락 시 stale free-slots. 시연 ACT 2 (거부 발화 → 캘린더 갱신) 검증 필수.

---

## Task 7: K2.3 측정 인프라 — entity_extraction endpoint 노출

baseline K2.3 0% (classify API slot 미반환). entity_extraction 단독 endpoint 추가 + .gstack-k2-runner 확장.

**Files:**
- Modify: `backend/app/api/routes/intents.py` (또는 신규 `entity_extraction.py`)
- Modify: `.gstack-k2-runner.py` (K2.3 새 endpoint 호출)

- [ ] **Step 7.1**: `POST /api/v1/intents/extract-entities` endpoint 추가 (현재 pipeline 의 entity_extraction 노드 wrapper)
- [ ] **Step 7.2**: response: `{"slots": {...}, "confidence": ...}`
- [ ] **Step 7.3**: runner 의 K2.3 측정 분기에서 새 endpoint 호출 + slot equality 검증
- [ ] **Step 7.4**: K2 runner 50개 재실행 → K2.3 비율 측정
- [ ] **Step 7.5**: Commit + push

---

## Task 8 (deferred): ml_place model 학습 + 배포

`data/training/train.py` 실행. 학습 시간 추정 어려움. 사용자 결정 후 진행.

산출물:
- `data/output/training/models/model_v2_no_sentiment.pkl`
- `data/output/features/place_features.csv`
- `data/output/kakao_map.csv`

회귀 위험: 학습 결과 모델이 시연 시나리오에 부적합 가능성. 시연 검증 필수.

---

## Task 9 (deferred): ACT 5.5 Option C — .env true 복원 + 안전망 검증

ACT 5.5 토글 시연 가치 회복. **선행 조건**: code-analyst 가 1·2차 안전망 (TimeBarSelector `restoredFromServer` ref + social.py `_is_explicit`) 회귀 없음 확인.

회귀 위험: TimeBar unmount race 회귀 가능성 (commit `558c57c` 이후). 시연 검증 필수, fail 시 즉시 `.env` 1줄 + restart 로 rollback.

---

## Task 10 (deferred): partial card prefetch (PR-V2.2)

사용자 발화 직후 background 로 vote_card 계산 시작 → 첫 카드 발행 latency 단축. spec v2 PR-V2.2 후보. 별 plan 필요.

---

## Phase 4 진입 절차

Plan 2 의 즉시/단순/중간 task (T1~T7) 완료 후:
1. qa-runtime 위임 — Phase 2 baseline 측정 절차 재실행
2. before/after 표 작성 (`docs/handoff/2026-06-02-phase4-result.md`)
3. 잔존 미달 영역 → 도우미 운영 안내문 + alarm rule
4. GO/NO-GO 결정 — 사용자 결정 gate

---

## Self-Review

### Spec coverage
- baseline FAIL 5건 (K1.1/K1.2/K1.3/K2.3/K3.1) 매핑:
  - K1.1 → T1 (timeout) + T5 (place 캐싱) + T6 (free-slots 캐싱) ✅
  - K1.2 → T1 (timeout) + T8 (ml model, deferred) ✅
  - K1.3 → T1 + T5 ✅
  - K2.3 → T7 ✅
  - K3.1 → BUG-27-1 fix (`be87e7b`) 완료 ✅
- silent-fail audit P1 15건 → T2/T3/T4 분할 ✅
- spec v2 PR 후보 (partial prefetch, ml 학습) → T8/T10 deferred ✅

### Placeholder scan
- "deferred" 명시 task (T8/T9/T10) 는 의도된 보류. 본 plan scope 외 명시.
- T7 의 entity_extraction endpoint 구현 코드는 backend 의 entity_extraction 노드 wrapper — 정확 구현은 task 진행 중 결정.

### Type consistency
- 각 task 의 Redis key 형식 (`maedeup:*`) 일관.
- TTL 명시 (place 30min, free-slots 5min, confirm lock 30s — BUG-27-1 fix 와 일치).

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-29-phase3-fix.md`.**

자율 풀가속 모드 — Task 1~7 sequential subagent dispatch. 각 task 후 qa-runtime 회귀 검증 + commit + push 자율. T8/T9/T10 는 사용자 결정 받고 별 trigger.

진행 순서: T1 → T2 → T3 → T4 → T5 → T6 → T7 → Phase 4 진입.
